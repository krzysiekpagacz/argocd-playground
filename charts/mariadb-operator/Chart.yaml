# cli command to inspect the chart: helm show chart mariadb-operator --repo https://helm.mariadb.com/mariadb-operator
apiVersion: v2
name: mariadb-operator
type: application
description: Helm Chart that deploys official MariaDB Operator
version: 0.1.0
dependencies:
  - name: mariadb-operator
    version: 25.10.2
    repository: "https://helm.mariadb.com/mariadb-operator"
